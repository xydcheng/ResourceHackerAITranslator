# ResourceHackerAITranslator release package

This package contains source code, documentation, sanitized config files, and the compiled EXE under `dist/`.

All `apiKey`, `appKey`, and `appSecret` fields in packaged config files have been cleared.
Fill your own keys locally before use.
